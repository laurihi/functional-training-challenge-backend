

spring:
  profiles: production
  datasource.url: jdbc:h2:mem:test-db
  datasource.username: sa
  datasource.password:
  datasource.driver-class-name: org.h2.Driver

---

spring:
  profiles: test

  datasource.url: jdbc:h2:mem:ftc-test-db
  datasource.username: sa
  datasource.password:
  datasource.driver-class-name: org.h2.Driver

